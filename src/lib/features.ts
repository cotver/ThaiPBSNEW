/** Set to true to show title playback actions and episode browsing again. */
export const ENABLE_TITLE_PLAYBACK = false;

/** Prefer audible trailer autoplay; browsers may still require a muted fallback. */
export const PREFER_TRAILER_SOUND = true;
